{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "minLength": 2,
      "maxLength": 2,
      "description": "state Area code. Two-letter US state code (e.g. CA, NY), required."
    }
  },
  "required": [
    "code"
  ]
}
